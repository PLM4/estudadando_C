#include <stdio.h>
#include <math.h>

int main()
{

    int k = 3;
    int l = 4;

    printf("O potencia de %d elevado a %d eh %lf\n", k, l, pow(k, l));

    int m = 4;

    printf("A raiz quadrada de %d x eh %lf\n", m, sqrt(m));

    return 0;
}